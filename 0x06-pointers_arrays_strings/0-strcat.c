#include <stdio.h>
#include "main.h"
/**
 * _strcat - concatenat
 * @dest: concatenates
 * @src: string
 * Return: none
 */
char *_strcat(char *dest, char *src)
{
printf("hie%s", dest);
printf("hie%s", src);
return (0);
}
