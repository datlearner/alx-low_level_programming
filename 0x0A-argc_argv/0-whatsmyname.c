#include <stdio.h>
#include "main.h"
/**
 * main - program name
 * @argc: argument count
 * @argv: argument vector
 * Return: (success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);
return (0);
}
