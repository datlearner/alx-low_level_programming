#include "main.h"

/**
 * _issuper - check for uppercase character
 * @c: function parameter
 * Return: 1 (success) 0 (failure)
 */

int _issuper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
